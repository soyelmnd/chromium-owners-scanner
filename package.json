{
  "name": "chromium-owners-scanner",
  "version": "0.0.1",
  "description": "An utility belt to assist teams working with Chromium's OWNERS files",
  "bin": {
    "goowners": "dist/stats.js",
    "goowners-stats": "dist/stats.js",
    "goowners-test-coverage": "dist/test-coverage.js"
  },
  "scripts": {
    "build": "tsc -p .",
    "stats": "npm run build && node dist/stats.js",
    "test-coverage": "npm run build && node dist/test-coverage.js"
  },
  "license": "ISC",
  "dependencies": {
    "console-table-printer": "^2.11.2",
    "jest": "^29.7.0",
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "@types/node": "^20.8.9",
    "typescript": "^5.2.2"
  }
}
